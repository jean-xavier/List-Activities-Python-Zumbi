distancia = float(input("Digite a distancia: "))
velocidade_media = float(input("Digite a velocidade media: "))

temp_estimado = distancia / velocidade_media;

print("O tempo estimado é %.2f minutos"%(temp_estimado))